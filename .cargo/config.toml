[alias]
# Run FE + BE together from repo root
# Usage: cargo dev
# This points to a tiny helper crate so starting dev mode does not compile
# the full backend before the watcher even begins.
dev = "run --manifest-path xtask/Cargo.toml"
